import 'package:flutter/material.dart';

class HomeCarosell extends StatelessWidget {
  const HomeCarosell({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
